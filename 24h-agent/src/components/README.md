# components

Reusable UI components that are not tied to a single product module.

Recommended initial files:

- `Button.tsx`: shared button variants.
- `Panel.tsx`: reusable panel shell.
- `StatusPill.tsx`: compact status label.
- `Timeline.tsx`: generic timeline primitive if multiple modules need it.

