# core

Pure business logic that should not depend on React.

Recommended initial files:

- `scheduler.ts`: schedule generation and rescheduling rules.
- `notificationRouter.ts`: interruption and notification routing logic.
- `autonomyPolicy.ts`: Auto, Ask, and Lock decision rules.
- `rhythmPolicy.ts`: rules for matching task type to user rhythm and state.

