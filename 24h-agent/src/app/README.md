# app

Application composition layer.

Recommended initial files:

- `App.tsx`: top-level React shell.
- `providers.tsx`: app-level context providers when needed.
- `routes.tsx`: route definitions when the demo grows beyond one screen.

