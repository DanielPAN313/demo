# data

Static demo data and mock fixtures for the pure frontend MVP.

Recommended initial files:

- `demoData.ts`: default tasks, schedules, user state, and external events.
- `mockTasks.ts`: task-only fixtures if task data grows.
- `mockEvents.ts`: external event fixtures such as GitHub issues and messages.

